<?php if (count($comments) != 0): ?>
	<h3>Comments on this page</h3>

	<?php foreach($comments as $comment): ?>
	<div><strong>Posted by:</strong> <?php echo $comment->name ?></div>
	<div><?php echo $comment->message ?></div>
	<hr>
	<?php endforeach; ?>
	
<?php else: ?>
	<div class="alert alert-info">
		<strong>No comment were posted</strong>
	</div>
<?php endif; ?>